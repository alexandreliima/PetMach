<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="PetMach.Mobile.ChatPage" xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" Title="Chat" BackgroundColor="{StaticResource Surface}">
    <Grid Padding="18" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="10">
        <Button Command="{Binding LoadOlderCommand}" Text="Carregar mensagens anteriores" />
        <CollectionView Grid.Row="1" ItemsSource="{Binding Messages}"><CollectionView.ItemTemplate><DataTemplate><Border Margin="0,4" Padding="12" BackgroundColor="White" Stroke="#E7D8CD" StrokeShape="RoundRectangle 14"><Label Text="{Binding Content}" /></Border></DataTemplate></CollectionView.ItemTemplate></CollectionView>
        <Grid Grid.Row="2" ColumnDefinitions="*,Auto" ColumnSpacing="8"><Entry MaxLength="2000" Placeholder="Escreva uma mensagem" Text="{Binding Draft}" /><Button Grid.Column="1" Command="{Binding SendCommand}" Text="Enviar" /></Grid>
        <Label Grid.Row="3" HorizontalTextAlignment="Center" Text="{Binding StatusMessage}" />
    </Grid>
</ContentPage>
