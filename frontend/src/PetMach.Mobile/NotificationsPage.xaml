<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="PetMach.Mobile.NotificationsPage" xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" Title="Notificações" BackgroundColor="{StaticResource Surface}">
    <Grid Padding="24" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="14">
        <Label FontAttributes="Bold" FontSize="30" Text="Notificações" />
        <CollectionView Grid.Row="1" ItemsSource="{Binding Notifications}" EmptyView="Nenhuma notificação por enquanto.">
            <CollectionView.ItemTemplate><DataTemplate><Border Margin="0,0,0,12" BackgroundColor="White" Padding="18" Stroke="#E7D8CD" StrokeShape="RoundRectangle 18"><Grid ColumnDefinitions="*,Auto"><VerticalStackLayout><Label FontAttributes="Bold" FontSize="19" Text="{Binding Title}" /><Label Text="{Binding Message}" TextColor="#6B5E56" /></VerticalStackLayout><Button Grid.Column="1" BackgroundColor="Transparent" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.MarkAsReadCommand}" CommandParameter="{Binding .}" Text="Marcar como lida" TextColor="{StaticResource Primary}" /></Grid></Border></DataTemplate></CollectionView.ItemTemplate>
        </CollectionView>
        <ActivityIndicator Grid.Row="2" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        <Label Grid.Row="3" HorizontalTextAlignment="Center" Text="{Binding StatusMessage}" TextColor="#7A3323" />
    </Grid>
</ContentPage>
